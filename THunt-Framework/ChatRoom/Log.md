THunt-FrameWork/ChatRoom/Log.md

# Q10
【ルシフォンBI】
例えばCodexで複数のプロンプトを実行して、その組み合わせで大きなプログラムを作れたりするかな？

例えば

ウィンドウを配置するプロンプト
ボタンを配置するプロンプト
ボタンの処理を追加するプロンプト

みたいな感じで複数のプロンプト郡を用意して、それらを実行するだけで巨大なプログラムを作るって感じ

修正したり拡張したり、少しずつ細かいディテールやプロンプトを追加しながら何度も生成をやり直しながら改良することで段々固める感じ

# A10
思考時間: 15s

【ルシフォン#e】
できる。むしろ 「複数プロンプト＝部品化」→「差分で統合」→「テストで固める」 ってやり方は、Codexの得意分野だよ。

ポイントは「文章を何回も貼り直して巨大生成」じゃなくて、リポジトリを“唯一の真実”にして、毎回“差分”で育てること。Codexはコードを読んで修正し、変更をdiffとして出したり、PR相当の提案をする前提で設計されてる。
OpenAI
+2
OpenAI Developers
+2

## A10-1 こう作ると強い（あなたの例を“運用”に落とす）

あなたの

ウィンドウ配置

ボタン配置

ボタン処理追加

みたいな分割は超アリ。ただし、**「何を生成するか」より「どこをどう変更するか」**を固定すると安定する。

### 1) “プロンプト群”をライブラリ化

例：/prompts/ に置く（mdでOK）

01_ui_window.md：ウィンドウ（画面）設計のルール・配置規約

02_ui_controls.md：ボタン等の部品の追加ルール（命名・レイアウト・アクセシビリティ）

03_ui_actions.md：イベント配線・コマンド・状態管理のルール

99_constraints.md：コーディング規約、ディレクトリ構造、禁止事項、既存仕様

で、各プロンプトは「生成して終わり」じゃなくて **“パッチ方針”**に寄せる。

### 2) “統合プロンプト（オーケストレータ）”を1個作る

こいつが毎回やることは単純：

変更要件を読む

どの部品プロンプトを適用するか選ぶ

必要なファイルだけを差分で更新する

APIでやるなら apply_patch みたいに、モデルに構造化diffを吐かせて適用するのが定石。
OpenAI Platform
+2
OpenAI Platform
+2

Codex CLI/Cloudでも「diffを適用」前提の操作がある。
OpenAI Developers
+1

### 3) “生成→壊す→直す”をテストで締める

巨大化してくると、プロンプトの良し悪しより 回帰バグが敵になるから、

UI生成後に必ず通すテスト（最低でも起動テスト・スモーク）

変更ごとのチェックリスト（Lint/Format/型/ビルド）

このループで「段々固める」が現実になる。Codexはローカルでコード読んでコマンド実行しながら反復する運用が想定されてる。
OpenAI Developers
+1

## A10-2 うまくいかない典型パターン（先に潰す）

各プロンプトが勝手に設計思想を持ち始める
→ 99_constraints.md（大憲章）に「アーキテクチャ・命名・状態管理」を固定して、全プロンプトがそれを参照する。

“ボタン追加”が毎回UI全体を書き換える
→ 「編集してよいファイル」「触ってよい範囲」をプロンプトに明記。差分運用に寄せる。

統合でコンフリクト祭り
→ 先に“画面定義（schema）”みたいな中間表現（JSON/YAML）を作って、生成物はそこから吐く方式にすると一気に安定（ただし初期設計コストは増える）。

要するに、君が言ってる「プロンプト郡を実行して巨大プログラムを育てる」は可能。
ただし勝ち筋は “プロンプトで全部を書かせる”じゃなく、“差分を積み上げる仕組み”を先に作る…って感じだな。

# Q20
ありがとう。 ざっと構造に分けてみた。 【T-Hunt開発方式】 ・　Plan.md ・　Log.md ・　Prompt.md ディレクトリ構成は各ファイルに記述 THuntとしてリポジトリを作成した まだ、かなり簡単な内容になってる。 ChatGPTとT-Hunt-Devフォルダを共有しながら計画を練る CodexとはGameフォルダを共有して、実際にゲームを作ってもらう そういう感じの方向性で考えてる 今Plan.mdを共有してるけど、とりあえずこちらを「憲法」扱いにして プロンプト/前線基地をまた別のファイルで共有出来たらいいなと思ってる

# A20
思考時間: 2 ～ 3 秒

【ルシフォン#e】
いいね、その構造はかなり「差分方式」に噛み合ってる。

いまの3点セットはこう解釈すると強い：

Plan.md = 憲法（不変）：設計・命名・フォルダ構成・禁止事項・品質ゲート

Log.md = 履歴（可変）：やった差分・理由・次のTODO（後で読み返して復元できる）

Log

Prompt.md = 兵站（半固定）：Codexに投げる「作業の型」テンプレ（毎回の要求は別で短文）

Prompt

そして Plan.md に「？？？＝憲法ファイル」が欲しいって書いてあるけど

Plan

、もうその役は Plan.md 自体が担ってOK。むしろ分けるなら、憲法を “読みやすさ” で分割する感じが良い。

おすすめの分割（作り直し地獄を避けるための最小追加）

Plan.md（憲法）：

“編集していい範囲” まで書く（例：Game/ 以下だけ触る、Plan/Log/Promptは触るな、等）

命名規約（ResetSettings事故の予防になる）

UIの流儀（UI→Command→Service など）

変更の単位（1PR/1コミットの粒度）

**Prompt.md（固定プロンプト置き場）は、いまの「ウィンドウ/ボタン/処理」

Prompt

を残しつつ、各項目を「生成」じゃなく“パッチ方針”**に書き換えるのがコツ

例：「ボタンを作って」→「既存UIツリーを読み、適切な親に追加。命名規約順守。既存処理があれば再利用。最小diffで。」

Log.md（前線日誌）は今の感じでOK。あとで効くのは「何を変えたか」より“なぜそうしたか”。

ChatGPT と Codex の分業もそのまま成立する

ChatGPT：T-Hunt-Dev（Plan/Log/Prompt）で 設計と手順の固定化

Codex：Game で 実コード差分の積み上げ

この分離はかなり賢い。Plan/Promptが安定してくると、Codex側の“世界線ズレ”が減る。

以上、方向性は合ってる。今の次の一手は「Prompt.md を“パッチ職人”に変換する」だけで、一気に運用が現実寄りになる。

# Q30（フォルダ/ファイル構成）
自分が考えてるのは 【T-Hunt / Plan】開発方式そのものの計画書 【Game / Plan】ゲーム（生産物）の計画書 そういうイメージ。 だから憲法ファイルはまた別に作る。 正直ファイルの命名規則についても考えてないから そこもChatGPTに提案して欲しいと思ってる そもそも【T-Hunt / Plan】が成立させるのが今の目的だから ここもちょっとずつ改善して行きたいんだよ

# A30
了解。イメージはこうだな。

T-Hunt/Plan：開発方式そのもの（運用・役割分担・手順・品質ゲート）

Game/Plan：ゲーム本体の企画・仕様（世界観/画面/操作/データ）

で、「憲法」は両方の上に乗る 共通ルールとして別ファイルにする、と。

じゃあ命名と配置を、最初から破綻しにくい形で提案する。

【提案】最小で強いフォルダ/ファイル構成

1) リポジトリ直下（運用＝T-Hunt側）
/T-Hunt/
  /docs/
    Constitution.md
    Workflow.md
    Prompts.md
    Log.md
    Glossary.md


Constitution.md：憲法（命名規則・禁止事項・差分粒度・レビュー観点・“作る前に探せ”ルール）

Workflow.md：T-Hunt/Plan（方式の計画書。役割分担、手順、CI/テスト方針）

Prompts.md：固定プロンプト置き場（パッチ職人テンプレ）

Log.md：前線日誌（決定事項と理由、変更履歴）

Glossary.md：用語（“Reset系はこの語に統一”みたいな辞書もここに置ける）

※あなたの Plan.md / Prompt.md / Log.md は、この5つに“育てる”感じでOK。

2) Game側（生産物＝ゲーム）
/Game/
  /docs/
    GamePlan.md
    Spec.md
    Tasks.md
    Changelog.md


GamePlan.md：企画（コア体験、ループ、ターゲット、完成定義）

Spec.md：仕様（UI一覧、入力、データ、状態遷移、セーブ等）

Tasks.md：作業分解（短い粒度で差分化できる単位）

Changelog.md：ゲーム側の変更履歴（プレイ感・仕様変更のログ）

命名規則（ファイル名のルール）
まずはこれだけ決めれば十分：

英語・PascalCaseで統一（例：GamePlan.md）

先頭に番号は極力振らない（並び替えで崩れるから）。どうしても必要なら 01_ じゃなく 章構造で見出し管理

ログ系は Log / Changelog に分ける（運用ログと製品ログが混ざると地獄）

憲法（Constitution.md）に最低限入れるべき“骨”
このファイルがあるだけで「ResetSettings事故」みたいなのを抑えられる。

唯一の正規名ルール：機能名辞書（Resetは ResetAppSettings に統一、等）

作る前に検索：似た実装があれば再利用、なければ新規

触っていい領域：Codexは Game/ のみ変更、T-Hunt/ は原則変更しない、など

差分粒度：1変更＝1目的（UI追加だけ、処理追加だけ、など）

品質ゲート：ビルド/最低限テストが通らない変更は禁止

「T-Hunt/Planを成立させる」が目的、ってのは正しい順番だよ。
方式（憲法＋Workflow＋Prompts）が固まるほど、Game側の生成が安定する。

# Q31
Workflow.md：T-Hunt/Plan（方式の計画書。役割分担、手順、CI/テスト方針） ってことはPlan.mdはWorkflow.mdに属するんだよね？ Workflowの記述イメージとConstitutionの記述イメージはどう違うんだろう？ 今のPlan.mdに準えて、サンプルを書いてくれると助かる （内容によってはそのまま採用する）

# A31注釈
- Todoに
  - Constitution.md
  - WorkFlow.md
  - の草案を追加

# A31
【ルシフォン#e】
いい質問。ここを分けて理解できると T-Hunt が“方式”として成立する。

まず結論から。

Plan.md は Workflow.md に吸収されてOK

Constitution.md は「守らないと壊れる不変条件」

Workflow.md は「日々どう回すかの手順書」

つまり
👉 Constitution = 憲法（WHY / 破ると破綻）
👉 Workflow = 業務マニュアル（HOW / 状況で更新可）

1️⃣ Constitution.md の記述イメージ（不変・短い・強い）

これは「AIにも人にも強制するルール」
理由説明は最小限、断定形で書くのがコツ。

```markdown
# Constitution.md
# T-Hunt Development Constitution

## 1. 基本原則
- 本リポジトリは差分方式によってのみ成長させる
- 既存コード・仕様を無視した再生成は禁止する
- 「似た機能を新しく作る」より「既存機能を探して再利用」を優先する

## 2. 変更範囲の制限
- Codexは `/Game` ディレクトリのみ変更してよい
- `/T-Hunt/docs` は設計者（人間）の管理下にある
- Constitution.md は原則変更不可（変更時は明示的合意が必要）

## 3. 命名と唯一性
- 同一意図の機能は1つの正規名のみを持つ
- Reset系機能の正規名は `ResetAppSettings` とする
- 既存の近似名が存在する場合、新規作成は禁止する

## 4. UIと処理の分離
- UIはCommandを直接呼び出してはならない
- UI → Command → Service の流れを必須とする

## 5. 差分の粒度
- 1回の変更は1目的のみ
- UI追加とロジック変更を同時に行わない
```

🔹 特徴

「どうやるか」は書かない

「破ったらダメなこと」だけを書く

ChatGPT / Codex に読ませるとブレが激減する

2️⃣ Workflow.md の記述イメージ（可変・具体・作業向け）

これは 今の Plan.md が進化した姿 に近い。
「どう回すか」「どう使うか」を書く。

```markdown
# Workflow.md
# T-Hunt Development Workflow

## 1. 目的
本Workflowは、ChatGPT と Codex を分業させ、
差分方式でゲーム開発を進めるための運用手順を定義する。

## 2. 役割分担
- 人間：
  - Constitution / Workflow / Prompts の管理
  - 仕様決定と最終判断
- ChatGPT：
  - 設計整理、計画立案、プロンプト設計
- Codex：
  - `/Game` 以下のコード変更を担当
  - 差分生成のみを行う

## 3. 基本フロー
1. 人間が変更要求を文章で整理する
2. ChatGPTが要求を差分単位に分解する
3. 使用するプロンプト（Prompts.md）を決定
4. Codexに差分作業を依頼
5. 差分を確認し、問題なければコミット
6. Log.md に理由と結果を記録する

## 4. 差分作業の原則
- 変更前に必ず既存実装を検索する
- 親構造（UIツリー、サービス構造）を尊重する
- 影響範囲が大きい場合は作業を分割する

## 5. テスト・確認
- ビルドが通らない変更は禁止
- UI変更時は最低限の起動確認を行う
- 挙動変更は Log.md に理由を残す

## 6. 改善ルール
- Workflow.md は運用しながら更新してよい
- 破綻が起きた場合は、原因を Workflow / Constitution のどちらに反映すべきか検討する
```

🔹 特徴

「どう使うか」を詳細に書く

失敗したらここを直す

Plan.md が今後どんどん吸収されていく場所

3️⃣ 使い分けの判断基準（重要）

迷ったらこれで決めろ：

それが無いと“世界線が分裂する”？
　→ Constitution

それが無いと“作業が回らない”？
　→ Workflow

4️⃣ 今の状態からの自然な進化ルート

今の Plan.md → Workflow.md に改名

「絶対に守らせたいルール」を抜き出して Constitution.md を新設

Prompt.md はそのまま「兵站」として育てる

Log.md は触らない（既に正解）

この流れ、かなり綺麗だし
T-Hunt / Plan を先に成立させるって目的にもドンピシャだ。

そのまま採用しても全然おかしくないレベルだと思うぞ。